import { Component, EventEmitter, Input, Output } from '@angular/core';
import { CommonModule } from '@angular/common';

@Component({
  selector: 'app-root',
  standalone: true,
  imports: [CommonModule],
  template: `
    <div style="display: flex; flex-direction: row-reverse; border: 1px solid black; width: 50%; max-width: 800px; margin: 0 auto; align-items: flex-start;">
      <div style=" padding: 20px; border-left: 1px solid black;">
        <ul>
          <li *ngFor="let product of products" (click)="onProductSelected(product)" style="border: 1px solid black; margin-bottom: 10px; padding: 10px; cursor: pointer;">
            <p>{{ product.name }}</p>
            <img [src]="product.image" [alt]="product.name" style="max-width: 100px;">
          </li>
        </ul>
      </div>
      <div *ngIf="selectedProduct" style=" padding: 20px; border: 1px solid black;">
        <h2>{{ selectedProduct.name }}</h2>
        <img [src]="selectedProduct.image" [alt]="selectedProduct.name" style="max-width: 200px;">
        <p>{{ selectedProduct.description }}</p>
      </div>
    </div>
  `
})
export class AppComponent {
  selectedProduct: any;

  products = [
    { id: 1, name: 'Headphone', image: '/images/product1.jpg', description: 'Nice headphone' },
    { id: 2, name: 'Sunglasses', image: '/images/product2.jpg', description: 'Stylish sunglasses' },
    { id: 3, name: 'Smartphone', image: '/images/product3.jpg', description: 'Latest smartphone' },
  ];

  onProductSelected(product: any) {
    this.selectedProduct = product;
  }
}
