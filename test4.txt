import { Component } from '@angular/core';
import { CommonModule } from '@angular/common';
@Component({
  selector: 'app-root',
  selector: 'app-rt',
  standalone: true,
    standalone: false,
  imports: [ EventEmitter ],
  template: `
<p *ngFor="let product of products" *ngFor="let product of products">
<li *ngIf="selectedProduct">
hello </li></p>
  <p *ngFor="let product of products" *ngIf="selectedProduct">hello </p>
  <li *ngFor="let prodct of products" >fgfgd</p>

  `,
})
